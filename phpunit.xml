<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheResult="false"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            Admin bootstrap values are pinned so the suite does not inherit them
            from whatever .env the machine happens to have. They match the
            fallbacks in AdminSeeder, which AdminSeederTest asserts against.
        -->
        <env name="ADMIN_EMAIL" value="admin@yallaspare.com"/>
        <env name="ADMIN_NAME" value="Super Admin"/>
        <env name="ADMIN_PASSWORD" value=""/>
        <!--
            Matches the non-production default in config/security.php. Admin
            tests assume 2FA is off and the ones covering it turn it on
            themselves, so leaving this to the environment made ~120 tests
            depend on a value .env.example legitimately sets the other way.
        -->
        <env name="ADMIN_TWO_FACTOR_ENABLED" value="false"/>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="OTPIQ_API_KEY" value=""/>
        <env name="OTPIQ_BASE_URL" value="https://api.otpiq.test/api"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
